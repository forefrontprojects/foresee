require "spec_helper"

RSpec.describe Foresee do
  it "has a version number" do
    expect(Foresee::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
